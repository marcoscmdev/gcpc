package dev.marcoscm.gcpc_backend.persistence.entity;

import jakarta.persistence.EnumType;
import jakarta.persistence.Enumerated;

public enum RolEnum {
    GUION, DIBUJO, COLOR, ENTINTADO, ROTULACION, OTRO
}
